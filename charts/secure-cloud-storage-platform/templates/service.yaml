apiVersion: v1
kind: Service
metadata:
  name: {{ include "secure-cloud-storage-platform.fullname" . }}
  labels:
    {{- include "secure-cloud-storage-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secure-cloud-storage-platform.selectorLabels" . | nindent 4 }}
